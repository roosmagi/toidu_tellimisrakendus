const Meals = () => {
    return (
        <ul id="meals">
            { 
            <p>Create a list of meals, using fetch data from backend</p>
                // list of meals
            }
        </ul>
    )
}

export default Meals